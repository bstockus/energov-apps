CREATE PROCEDURE [globalsetup].[USP_EXAMTYPE_SELECT_LOOKUP]
AS
	SET NOCOUNT ON;
	SELECT		[dbo].[EXAMTYPE].[EXAMTYPEID],
				[dbo].[EXAMTYPE].[NAME]
	FROM		[dbo].[EXAMTYPE]
	ORDER BY	[dbo].[EXAMTYPE].[NAME]