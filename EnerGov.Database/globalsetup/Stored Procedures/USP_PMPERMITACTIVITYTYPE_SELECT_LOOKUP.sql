CREATE PROCEDURE [globalsetup].[USP_PMPERMITACTIVITYTYPE_SELECT_LOOKUP]
AS
	SET NOCOUNT ON;
	SELECT		[dbo].[PMPERMITACTIVITYTYPE].[PMPERMITACTIVITYTYPEID],
				[dbo].[PMPERMITACTIVITYTYPE].[NAME]
	FROM		[dbo].[PMPERMITACTIVITYTYPE]
	ORDER BY	[dbo].[PMPERMITACTIVITYTYPE].[NAME]