CREATE PROCEDURE [globalsetup].[USP_SERVICETASKHISTORYACTION_RESULT_FILTERS_BYSERVICETASKHISTORYDETAILID]
(
	@SERVICETASKHISTORYDETAILID	CHAR(36)
)
AS
BEGIN
	SET NOCOUNT ON;

	SELECT DISTINCT	[dbo].[SERVICETASKHISTORYACTION].[RESULT]
	FROM 
		[dbo].[SERVICETASKHISTORYACTION]
	WHERE
		[dbo].[SERVICETASKHISTORYACTION].[SERVICETASKHISTORYDETAILID] = @SERVICETASKHISTORYDETAILID

END