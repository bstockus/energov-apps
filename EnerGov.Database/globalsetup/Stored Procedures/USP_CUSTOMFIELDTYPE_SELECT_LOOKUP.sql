CREATE PROCEDURE [globalsetup].[USP_CUSTOMFIELDTYPE_SELECT_LOOKUP]
AS
BEGIN
	SET NOCOUNT ON;
	SELECT 
		[dbo].[CUSTOMFIELDTYPE].[ICUSTOMFIELDTYPE],
		[dbo].[CUSTOMFIELDTYPE].[SNAME]
	FROM [dbo].[CUSTOMFIELDTYPE]
	ORDER BY [dbo].[CUSTOMFIELDTYPE].[SNAME]
END