CREATE PROCEDURE [globalsetup].[USP_CACPITYPE_SELECT_LOOKUP]
AS
SET NOCOUNT ON;
SELECT		[dbo].[CACPITYPE].[CACPITYPEID],
			[dbo].[CACPITYPE].[NAME]
FROM		[dbo].[CACPITYPE]
ORDER BY	[dbo].[CACPITYPE].[NAME]