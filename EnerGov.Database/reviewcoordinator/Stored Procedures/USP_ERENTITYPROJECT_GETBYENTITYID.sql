CREATE PROCEDURE [reviewcoordinator].[USP_ERENTITYPROJECT_GETBYENTITYID]
(
	@ENTITYID VARCHAR(36)
)
AS
BEGIN
SET NOCOUNT ON;
SELECT 
	ERENTITYPROJECTID,
	PROJECTNAME,
	COALESCE(PMPERMITID,PLPLANID) ENTITYID
FROM dbo.ERENTITYPROJECT
WHERE
	PMPERMITID = @ENTITYID or PLPLANID = @ENTITYID
END