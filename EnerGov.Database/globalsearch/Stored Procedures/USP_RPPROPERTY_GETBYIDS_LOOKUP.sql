CREATE PROCEDURE [globalsearch].[USP_RPPROPERTY_GETBYIDS_LOOKUP]
(
	@IDS RecordIDs READONLY -- RPPROPERTYIDS
)
AS
BEGIN
SET NOCOUNT ON;

SELECT 
	[RPPROPERTYID],
	[PROPERTYNUMBER]
FROM [dbo].[RPPROPERTY]
WHERE
	 [RPPROPERTY].[RPPROPERTYID] IN (SELECT [RECORDID] FROM @IDS)

END