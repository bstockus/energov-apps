CREATE PROCEDURE [globalsearch].[USP_DEPARTMENT_SELECT_LOOKUP]
AS
BEGIN
SET NOCOUNT ON;
	SELECT 
		[DEPARTMENT].[DEPARTMENTID],
		[DEPARTMENT].[NAME] 
	FROM 
		[dbo].[DEPARTMENT] WITH (NOLOCK)
		ORDER BY [DEPARTMENT].[NAME] 
END