CREATE PROCEDURE [systemsetup].[USP_BLCONTACTTYPE_SELECT_LOOKUP]
AS
BEGIN
	SET NOCOUNT ON;

	SELECT		[dbo].[BLCONTACTTYPE].[BLCONTACTTYPEID],
				[dbo].[BLCONTACTTYPE].[NAME]
	FROM		[dbo].[BLCONTACTTYPE]
	ORDER BY	[dbo].[BLCONTACTTYPE].[NAME]
END