CREATE PROCEDURE [systemsetup].[USP_JURISDICTION_SELECT_LOOKUP]
AS
BEGIN
	SET NOCOUNT ON;

	SELECT		[dbo].[JURISDICTION].[JURISDICTIONID],
				[dbo].[JURISDICTION].[NAME],
				[dbo].[JURISDICTION].[CAFINANCIALINTEGRATIONSETUPID]
	FROM		[dbo].[JURISDICTION]
	ORDER BY	[dbo].[JURISDICTION].[NAME]
END