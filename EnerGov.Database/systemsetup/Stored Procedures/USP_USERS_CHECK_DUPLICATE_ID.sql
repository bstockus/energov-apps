CREATE PROCEDURE [systemsetup].[USP_USERS_CHECK_DUPLICATE_ID]
(
	@SUSERGUID CHAR(36),
	@ID NVARCHAR(80)
)
AS
SET NOCOUNT ON;
SELECT	[dbo].[USERS].[SUSERGUID],
		[dbo].[USERS].[ID]				
FROM	[dbo].[USERS] WITH (NOLOCK)
WHERE		
	[dbo].[USERS].[ID] = @ID AND [dbo].[USERS].[SUSERGUID] <> @SUSERGUID