CREATE PROCEDURE [systemsetup].[USP_SUBMENUS_SELECT_LOOKUP]
AS
SET NOCOUNT ON;
SELECT 
	[dbo].[SUBMENUS].[SSUBMENUGUID],
	[dbo].[SUBMENUS].[SDESCRIPTION],
	[dbo].[SUBMENUS].[FKMENUGUID]
FROM [dbo].[SUBMENUS]
ORDER BY [dbo].[SUBMENUS].[IORDER] ASC