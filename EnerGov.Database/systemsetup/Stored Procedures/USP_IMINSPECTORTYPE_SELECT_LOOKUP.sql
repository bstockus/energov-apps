CREATE PROCEDURE [systemsetup].[USP_IMINSPECTORTYPE_SELECT_LOOKUP]
AS
BEGIN
	SET NOCOUNT ON;
	
	SELECT		[dbo].[IMINSPECTORTYPE].[IMINSPECTORTYPEID],
				[dbo].[IMINSPECTORTYPE].[SNAME]
	FROM		[dbo].[IMINSPECTORTYPE]
	ORDER BY	[dbo].[IMINSPECTORTYPE].[SNAME]
END