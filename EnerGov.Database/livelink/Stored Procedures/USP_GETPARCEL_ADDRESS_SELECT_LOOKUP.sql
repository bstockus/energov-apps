CREATE PROCEDURE [livelink].[USP_GETPARCEL_ADDRESS_SELECT_LOOKUP]
(
	@ADDRESSID CHAR(36)
)
AS
BEGIN
SET NOCOUNT ON;
SELECT 
		[PARCELADDRESS].[GISADDRESSID]
FROM [dbo].[PARCELADDRESS]

WHERE [PARCELADDRESS].[ADDRESSID] = @ADDRESSID
	
END