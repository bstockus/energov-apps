CREATE PROCEDURE [permitsetup].[USP_PMPERMITSTATUS_SELECT_LOOKUP]
AS
	SET NOCOUNT ON;

	SELECT 		[dbo].[PMPERMITSTATUS].[PMPERMITSTATUSID],
				[dbo].[PMPERMITSTATUS].[NAME]
	FROM		[dbo].[PMPERMITSTATUS]
	ORDER BY	[dbo].[PMPERMITSTATUS].[NAME]