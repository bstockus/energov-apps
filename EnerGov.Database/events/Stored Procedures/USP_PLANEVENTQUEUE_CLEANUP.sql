CREATE PROCEDURE [events].[USP_PLANEVENTQUEUE_CLEANUP]
@PROCESSEDDATELIMIT DATETIME
AS
BEGIN
	SET NOCOUNT ON;

	DELETE FROM [dbo].[PLANEVENTQUEUE] 
					WHERE [dbo].[PLANEVENTQUEUE].[PROCESSEDDATE] < @PROCESSEDDATELIMIT
		
END