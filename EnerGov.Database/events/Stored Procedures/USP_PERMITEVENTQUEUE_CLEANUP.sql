CREATE PROCEDURE [events].[USP_PERMITEVENTQUEUE_CLEANUP]
@PROCESSEDDATELIMIT DATETIME
AS
BEGIN
	SET NOCOUNT ON;

	DELETE FROM [dbo].[PERMITEVENTQUEUE] 
					WHERE [dbo].[PERMITEVENTQUEUE].[PROCESSEDDATE] < @PROCESSEDDATELIMIT
		
END