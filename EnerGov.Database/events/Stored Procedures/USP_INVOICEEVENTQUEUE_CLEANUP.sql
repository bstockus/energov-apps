CREATE PROCEDURE [events].[USP_INVOICEEVENTQUEUE_CLEANUP]
@PROCESSEDDATELIMIT DATETIME
AS
BEGIN
	SET NOCOUNT ON;

	DELETE FROM [dbo].[INVOICEEVENTQUEUE] 
					WHERE [dbo].[INVOICEEVENTQUEUE].[PROCESSEDDATE] < @PROCESSEDDATELIMIT
		
END