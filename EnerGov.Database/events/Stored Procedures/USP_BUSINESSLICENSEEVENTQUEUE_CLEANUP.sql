CREATE PROCEDURE [events].[USP_BUSINESSLICENSEEVENTQUEUE_CLEANUP]
@PROCESSEDDATELIMIT DATETIME
AS
BEGIN
	SET NOCOUNT ON;

	DELETE FROM [dbo].[BUSINESSLICENSEEVENTQUEUE] 
					WHERE [dbo].[BUSINESSLICENSEEVENTQUEUE].[PROCESSEDDATE] < @PROCESSEDDATELIMIT
		
END