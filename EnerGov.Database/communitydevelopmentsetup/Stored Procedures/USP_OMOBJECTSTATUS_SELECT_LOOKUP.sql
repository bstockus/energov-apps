CREATE PROCEDURE [communitydevelopmentsetup].[USP_OMOBJECTSTATUS_SELECT_LOOKUP]

AS
	SET NOCOUNT ON;
	SELECT		[dbo].[OMOBJECTSTATUS].[OMOBJECTSTATUSID],
				[dbo].[OMOBJECTSTATUS].[NAME]
	FROM		[dbo].[OMOBJECTSTATUS]
	ORDER BY	[dbo].[OMOBJECTSTATUS].[NAME]