CREATE PROCEDURE [common].[USP_WORKDAYS]

AS

SET NOCOUNT ON;

SELECT
	[dbo].[WORKWEEK].[DAYOFWEEK]
FROM [dbo].[WORKWEEK]
WHERE [dbo].[WORKWEEK].[ISWORKINGDAY] = 1
ORDER BY [dbo].[WORKWEEK].[DAYOFWEEK]