CREATE PROCEDURE [common].[USP_USERSETTINGS_GET]
	@SETTINGNAMES AS RECORDNAMES READONLY
AS
BEGIN

SET NOCOUNT ON;

SELECT USERSETTING.USERSETTINGID, USERSETTING.NAME
FROM USERSETTING 
WHERE USERSETTING.NAME IN (SELECT NAME FROM @SETTINGNAMES)

END