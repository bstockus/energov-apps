CREATE PROCEDURE [common].[USP_RECURRENCE_GETBYID]
	@ID CHAR(36)
AS
BEGIN
	DECLARE @RECURRENCEIDS AS RecordIds

	INSERT INTO @RECURRENCEIDS
	SELECT @ID

	EXEC [common].[USP_RECURRENCE_GETBYIDS] @RECURRENCEIDS
END