CREATE PROCEDURE [common].[USP_SYSTEMMODULE_SELECT_LOOKUP]
AS
BEGIN
	SET NOCOUNT ON;
	SELECT	[dbo].[SYSTEMMODULE].[SYSTEMMODULEID],
			[dbo].[SYSTEMMODULE].[MODULENAME],
			[dbo].[SYSTEMMODULE].[ISHTMLDEPRICATED]
	FROM [dbo].[SYSTEMMODULE]
	ORDER BY [dbo].[SYSTEMMODULE].[MODULENAME]
END