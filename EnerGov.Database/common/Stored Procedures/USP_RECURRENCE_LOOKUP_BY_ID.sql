CREATE PROCEDURE [common].[USP_RECURRENCE_LOOKUP_BY_ID]
	@ID CHAR(36)
AS
BEGIN
	SET NOCOUNT ON;

	SELECT
	[dbo].RECURRENCE.RECURRENCEID,
	[dbo].RECURRENCE.NAME
	FROM [dbo].RECURRENCE
	WHERE [dbo].RECURRENCE.RECURRENCEID = @ID
END