CREATE PROCEDURE [common].[USP_USERNOTIFICATION_COUNT]
(
	@USER_ID AS CHAR(36),
	@UNREAD	 AS BIT	= 1
)	
AS
BEGIN
	SELECT COUNT(*) AS USERNOTIFICATIONCOUNT 
	FROM [dbo].[USERNOTIFICATION] WHERE [USERID] = @USER_ID AND (@UNREAD = 0 OR [dbo].[USERNOTIFICATION].DATEREAD IS NULL)
END