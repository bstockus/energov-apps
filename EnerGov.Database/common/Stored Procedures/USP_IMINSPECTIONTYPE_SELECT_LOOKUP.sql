CREATE PROCEDURE [common].[USP_IMINSPECTIONTYPE_SELECT_LOOKUP]
AS
BEGIN
	SET NOCOUNT ON;
	SELECT		[dbo].[IMINSPECTIONTYPE].[IMINSPECTIONTYPEID],
				[dbo].[IMINSPECTIONTYPE].[NAME]
	FROM		[dbo].[IMINSPECTIONTYPE]
	ORDER BY	[dbo].[IMINSPECTIONTYPE].[NAME]
END