CREATE PROCEDURE [common].[USP_USERNOTIFICATION_UPDATE]
(
	@USERID CHAR(36),
	@NOTIFICATIONSID CHAR(36),
	@DATEREAD DATETIME
)
AS

UPDATE [dbo].[USERNOTIFICATION] SET DATEREAD = @DATEREAD WHERE USERID = @USERID AND NOTIFICATIONSID = @NOTIFICATIONSID