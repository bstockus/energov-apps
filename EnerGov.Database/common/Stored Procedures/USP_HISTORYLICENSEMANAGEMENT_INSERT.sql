CREATE PROCEDURE [common].[USP_HISTORYLICENSEMANAGEMENT_INSERT]
(
	@ID CHAR(36),
	@ROWVERSION INT,
	@CHANGEDON DATETIME,
	@CHANGEDBY CHAR(36),
	@FIELDNAME NVARCHAR(250),
	@OLDVALUE NVARCHAR(MAX),
	@NEWVALUE NVARCHAR(MAX),
	@ADDITIONALINFO NVARCHAR(MAX)
)
AS
DECLARE @OUTPUTTABLE as TABLE([HISTORYLICENSEMANAGEMENTID] int)
INSERT INTO [dbo].[HISTORYLICENSEMANAGEMENT](
	[ID],
	[ROWVERSION],
	[CHANGEDON],
	[CHANGEDBY],
	[FIELDNAME],
	[OLDVALUE],
	[NEWVALUE],
	[ADDITIONALINFO]
)
OUTPUT inserted.[HISTORYLICENSEMANAGEMENTID] INTO @OUTPUTTABLE
VALUES
(
	@ID,
	@ROWVERSION,
	@CHANGEDON,
	@CHANGEDBY,
	@FIELDNAME,
	@OLDVALUE,
	@NEWVALUE,
	@ADDITIONALINFO
)
SELECT * FROM @OUTPUTTABLE