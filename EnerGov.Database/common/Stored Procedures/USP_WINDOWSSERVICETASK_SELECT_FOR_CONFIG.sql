CREATE PROCEDURE [common].[USP_WINDOWSSERVICETASK_SELECT_FOR_CONFIG]
AS

SET NOCOUNT ON;
SELECT 
    [dbo].[WINDOWSSERVICETASK].[WINDOWSSERVICETASKID], 
    [dbo].[WINDOWSSERVICETASK].[NAME], 
    [dbo].[WINDOWSSERVICETASK].[ISENABLED], 
    [dbo].[WINDOWSSERVICETASK].[ISREADY]
FROM [dbo].[WINDOWSSERVICETASK]