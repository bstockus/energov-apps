CREATE PROCEDURE [common].[USP_TASKTYPE_SELECT_LOOKUP]
AS
	SET NOCOUNT ON;
	SELECT		[dbo].[TASKTYPE].[TASKTYPEID],
				[dbo].[TASKTYPE].[NAME]
	FROM		[dbo].[TASKTYPE]
	ORDER BY	[dbo].[TASKTYPE].[NAME]