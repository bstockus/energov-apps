CREATE PROCEDURE [energovnotifications].[USP_GLOBALHEARINGTYPE_GETALL]
AS
BEGIN
SET NOCOUNT ON;
	SELECT DISTINCT	
	[dbo].[HEARINGTYPE].[HEARINGTYPEID],
	[dbo].[HEARINGTYPE].[NAME]
	FROM [dbo].[HEARINGTYPE]
	ORDER BY [dbo].[HEARINGTYPE].[NAME]
END