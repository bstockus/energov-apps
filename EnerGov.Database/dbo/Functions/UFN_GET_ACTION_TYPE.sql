CREATE FUNCTION [dbo].[UFN_GET_ACTION_TYPE] (@FIELDNAME VARCHAR(MAX))
RETURNS INT

AS
BEGIN   
	RETURN (SELECT CASE WHEN @FIELDNAME LIKE '%Added%' THEN 1
						WHEN @FIELDNAME LIKE '%Deleted%' THEN 3
						ELSE 2 END )
END