CREATE PROCEDURE [dbo].[USP_RPLANDLORDLICENSETYPE_INSERT]
(
	@RPLANDLORDLICENSETYPEID CHAR(36),
	@NAME NVARCHAR(50),
	@DESCRIPTION NVARCHAR(MAX),
	@FRIENDLYNAME NVARCHAR(100),
	@PREFIX NVARCHAR(10),
	@DEFAULTSTATUSID CHAR(36),
	@CUSTOMFIELDLAYOUTID CHAR(36),
	@CAFEETEMPLATEID CHAR(36),
	@ACTIVE BIT,
	@BLEINFORMATID INT,
	@LASTCHANGEDBY CHAR(36),
	@LASTCHANGEDON DATETIME,
	@ROWVERSION INT
)
AS
DECLARE @OUTPUTTABLE as TABLE([ROWVERSION]  int)
INSERT INTO [dbo].[RPLANDLORDLICENSETYPE](
	[RPLANDLORDLICENSETYPEID],
	[NAME],
	[DESCRIPTION],
	[FRIENDLYNAME],
	[PREFIX],
	[DEFAULTSTATUSID],
	[CUSTOMFIELDLAYOUTID],
	[CAFEETEMPLATEID],
	[ACTIVE],
	[BLEINFORMATID],
	[LASTCHANGEDBY],
	[LASTCHANGEDON],
	[ROWVERSION]
)
OUTPUT inserted.[ROWVERSION] INTO @OUTPUTTABLE
VALUES
(
	@RPLANDLORDLICENSETYPEID,
	@NAME,
	@DESCRIPTION,
	@FRIENDLYNAME,
	@PREFIX,
	@DEFAULTSTATUSID,
	@CUSTOMFIELDLAYOUTID,
	@CAFEETEMPLATEID,
	@ACTIVE,
	@BLEINFORMATID,
	@LASTCHANGEDBY,
	@LASTCHANGEDON,
	@ROWVERSION
)
SELECT * FROM @OUTPUTTABLE