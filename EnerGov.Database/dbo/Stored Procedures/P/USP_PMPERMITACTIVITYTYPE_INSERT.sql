CREATE PROCEDURE [dbo].[USP_PMPERMITACTIVITYTYPE_INSERT]
(
	@PMPERMITACTIVITYTYPEID CHAR(36),
	@NAME NVARCHAR(50),
	@DESCRIPTION NVARCHAR(MAX),
	@CANEDIT BIT,
	@PREFIXID NVARCHAR(10),
	@ALLOWDUPLICATE BIT,
	@CREATEID BIT,
	@CUSTOMFIELDID CHAR(36),
	@LASTCHANGEDBY CHAR(36),
	@LASTCHANGEDON DATETIME,
	@ROWVERSION INT
)
AS
DECLARE @OUTPUTTABLE as TABLE([ROWVERSION]  int)
INSERT INTO [dbo].[PMPERMITACTIVITYTYPE](
	[PMPERMITACTIVITYTYPEID],
	[NAME],
	[DESCRIPTION],
	[CANEDIT],
	[PREFIXID],
	[ALLOWDUPLICATE],
	[CREATEID],
	[CUSTOMFIELDID],
	[LASTCHANGEDBY],
	[LASTCHANGEDON],
	[ROWVERSION]
)
OUTPUT inserted.[ROWVERSION] INTO @OUTPUTTABLE
VALUES
(
	@PMPERMITACTIVITYTYPEID,
	@NAME,
	@DESCRIPTION,
	@CANEDIT,
	@PREFIXID,
	@ALLOWDUPLICATE,
	@CREATEID,
	@CUSTOMFIELDID,
	@LASTCHANGEDBY,
	@LASTCHANGEDON,
	@ROWVERSION
)
SELECT * FROM @OUTPUTTABLE