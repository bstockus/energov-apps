


CREATE PROCEDURE [dbo].[RPT_PM_PERMIT_DETAIL_REPORT_PARCEL]
@PERMITID AS VARCHAR(36)
AS
SELECT PARCEL.PARCELNUMBER, PMPERMITPARCEL.MAIN, PMPERMITPARCEL.PARCELID
FROM PMPERMITPARCEL 
INNER JOIN PARCEL ON PMPERMITPARCEL.PARCELID = PARCEL.PARCELID
WHERE PMPERMITPARCEL.PMPERMITID = @PERMITID


