


CREATE PROCEDURE [dbo].[RPT_PM_PERMIT_DETAIL_REPORT_ZONE]
@PERMITID AS VARCHAR(36)
AS
SELECT PMPERMITZONE.MAIN, ZONE.ZONECODE, ZONE.NAME, PMPERMITZONE.ZONEID
FROM PMPERMITZONE 
INNER JOIN ZONE ON PMPERMITZONE.ZONEID = ZONE.ZONEID
WHERE PMPERMITZONE.PMPERMITID = @PERMITID


