

CREATE PROCEDURE [dbo].[rpt_PL_SR_Plan_Detail_Report_Zone]
@PLPLANID AS VARCHAR(36)
AS
SELECT PLPLANZONE.MAIN, ZONE.ZONECODE, ZONE.NAME, PLPLANZONE.ZONEID
FROM PLPLANZONE 
INNER JOIN ZONE ON PLPLANZONE.ZONEID = ZONE.ZONEID
WHERE PLPLANZONE.PLPLANID = @PLPLANID

