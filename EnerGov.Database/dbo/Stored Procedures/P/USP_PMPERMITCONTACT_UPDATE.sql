CREATE PROCEDURE [dbo].[USP_PMPERMITCONTACT_UPDATE]
(	
	@GLOBALENTITYID CHAR(36),
	@OLDGLOBALENTITYID CHAR(36)
)
AS

UPDATE [dbo].[PMPERMITCONTACT] SET	
	[GLOBALENTITYID] = @OLDGLOBALENTITYID

WHERE
	[GLOBALENTITYID] = @GLOBALENTITYID