CREATE PROCEDURE [dbo].[USP_TXREMITSTATUS_DELETE]
(
	@TXREMITSTATUSID CHAR(36),
	@ROWVERSION INT
)
AS
DELETE FROM [dbo].[TXREMITSTATUS]
WHERE
	[TXREMITSTATUSID] = @TXREMITSTATUSID AND 
	[ROWVERSION]= @ROWVERSION