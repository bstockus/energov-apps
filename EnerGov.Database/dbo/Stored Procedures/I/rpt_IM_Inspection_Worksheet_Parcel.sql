

CREATE PROCEDURE [dbo].[rpt_IM_Inspection_Worksheet_Parcel]
@INSPECTIONID AS VARCHAR(36)
AS
SELECT PARCEL.PARCELNUMBER AS ParcelNumber
FROM IMINSPECTIONPARCEL 
INNER JOIN PARCEL ON IMINSPECTIONPARCEL.PARCELID = PARCEL.PARCELID
WHERE IMINSPECTIONPARCEL.IMINSPECTIONID = @INSPECTIONID

