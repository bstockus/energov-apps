CREATE PROCEDURE [dbo].[USP_IMINSPECTIONZONE_UPDATE]
(
	@IMINSPECTIONZONEID CHAR(36),
	@NAME NVARCHAR(50),
	@DESCRIPTION NVARCHAR(MAX),
	@DEFAULTTRAVELTIME DECIMAL(18,4),
	@LASTCHANGEDBY CHAR(36),
	@LASTCHANGEDON DATETIME,
	@ROWVERSION INT
)
AS
DECLARE @OUTPUTTABLE as TABLE([ROWVERSION]  int)
UPDATE [dbo].[IMINSPECTIONZONE] SET
	[NAME] = @NAME,
	[DESCRIPTION] = @DESCRIPTION,
	[DEFAULTTRAVELTIME] = @DEFAULTTRAVELTIME,
	[LASTCHANGEDBY] = @LASTCHANGEDBY,
	[LASTCHANGEDON] = @LASTCHANGEDON,
	[ROWVERSION] = @ROWVERSION + 1
OUTPUT inserted.[ROWVERSION] INTO @OUTPUTTABLE
WHERE
	[IMINSPECTIONZONEID] = @IMINSPECTIONZONEID AND 
	[ROWVERSION]= @ROWVERSION

SELECT * FROM @OUTPUTTABLE