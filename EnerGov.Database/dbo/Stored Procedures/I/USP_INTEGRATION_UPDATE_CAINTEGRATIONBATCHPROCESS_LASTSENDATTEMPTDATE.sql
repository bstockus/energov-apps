CREATE PROCEDURE [dbo].[USP_INTEGRATION_UPDATE_CAINTEGRATIONBATCHPROCESS_LASTSENDATTEMPTDATE]
	@LASTSENDATTEMPTDATE AS DATETIME,
	@BATCH_ID AS NVARCHAR(50)
AS

BEGIN

UPDATE CAINTEGRATIONBATCHPROCESS 
SET LASTSENDATTEMPTDATE = @LASTSENDATTEMPTDATE 
WHERE FINANCEBATCHID = @BATCH_ID

END