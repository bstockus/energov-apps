CREATE PROCEDURE [dbo].[USP_CACPIREFERENCEDATE_INSERT]
(
	@CACPIREFERENCEDATEID CHAR(36),
	@CAMODULEID INT,
	@DATEOBJECTTYPEID INT,
	@DATEOBJECT NVARCHAR(255),
	@DATEOBJECTFRIENDLYNAME NVARCHAR(500),
	@ISDEFAULT BIT,
	@LASTCHANGEDBY CHAR(36),
	@LASTCHANGEDON DATETIME,
	@ROWVERSION INT
)
AS
DECLARE @OUTPUTTABLE as TABLE([ROWVERSION]  int)
INSERT INTO [dbo].[CACPIREFERENCEDATE](
	[CACPIREFERENCEDATEID],
	[CAMODULEID],
	[DATEOBJECTTYPEID],
	[DATEOBJECT],
	[DATEOBJECTFRIENDLYNAME],
	[ISDEFAULT],
	[LASTCHANGEDBY],
	[LASTCHANGEDON],
	[ROWVERSION]
)
OUTPUT inserted.[ROWVERSION] INTO @OUTPUTTABLE
VALUES
(
	@CACPIREFERENCEDATEID,
	@CAMODULEID,
	@DATEOBJECTTYPEID,
	@DATEOBJECT,
	@DATEOBJECTFRIENDLYNAME,
	@ISDEFAULT,
	@LASTCHANGEDBY,
	@LASTCHANGEDON,
	@ROWVERSION
)
SELECT * FROM @OUTPUTTABLE