CREATE PROCEDURE [dbo].[USP_COLICENSECERTIFICATIONTYPE_INSERT]
(
	@COSIMPLELICCERTTYPEID CHAR(36),
	@NAME NVARCHAR(255),
	@SUBJECTTOLICENSEVALIDATION BIT,
	@LASTCHANGEDBY CHAR(36),
	@LASTCHANGEDON DATETIME,
	@ROWVERSION INT
)
AS
DECLARE @OUTPUTTABLE as TABLE([ROWVERSION]  int)
INSERT INTO [dbo].[COLICENSECERTIFICATIONTYPE](
	[COSIMPLELICCERTTYPEID],
	[NAME],
	[SUBJECTTOLICENSEVALIDATION],
	[LASTCHANGEDBY],
	[LASTCHANGEDON],
	[ROWVERSION]
)
OUTPUT inserted.[ROWVERSION] INTO @OUTPUTTABLE
VALUES
(
	@COSIMPLELICCERTTYPEID,
	@NAME,
	@SUBJECTTOLICENSEVALIDATION,
	@LASTCHANGEDBY,
	@LASTCHANGEDON,
	@ROWVERSION
)
SELECT * FROM @OUTPUTTABLE