CREATE PROCEDURE [dbo].[USP_COLICENSECERTIFICATIONTYPE_UPDATE]
(
	@COSIMPLELICCERTTYPEID CHAR(36),
	@NAME NVARCHAR(255),
	@SUBJECTTOLICENSEVALIDATION BIT,
	@LASTCHANGEDBY CHAR(36),
	@LASTCHANGEDON DATETIME,
	@ROWVERSION INT
)
AS
DECLARE @OUTPUTTABLE as TABLE([ROWVERSION]  int)
UPDATE [dbo].[COLICENSECERTIFICATIONTYPE] SET
	[NAME] = @NAME,
	[SUBJECTTOLICENSEVALIDATION] = @SUBJECTTOLICENSEVALIDATION,
	[LASTCHANGEDBY] = @LASTCHANGEDBY,
	[LASTCHANGEDON] = @LASTCHANGEDON,
	[ROWVERSION] = @ROWVERSION + 1
OUTPUT inserted.[ROWVERSION] INTO @OUTPUTTABLE
WHERE
	[COSIMPLELICCERTTYPEID] = @COSIMPLELICCERTTYPEID AND 
	[ROWVERSION]= @ROWVERSION

SELECT * FROM @OUTPUTTABLE