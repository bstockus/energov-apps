CREATE PROCEDURE [dbo].[USP_USERSESSIONS_INSERT_USERSESSIONS]
    @SessionId CHAR(36),
	@UserId CHAR(36),
	@LoginTime DATETIME,
	@IsLoginSuccessful BIT	
AS
BEGIN
  INSERT INTO USERSESSIONS (SESSIONID, USERID, LOGIN, ISLOGINSUCCESSFUL) VALUES (@SessionId, @UserId, @LoginTime, @IsLoginSuccessful)
END