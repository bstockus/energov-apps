CREATE PROCEDURE [dbo].[SP_ELASTICSEARCHOBJECT_FREEDEAD] (
	@TIMEOUT INT
) AS
BEGIN
	UPDATE [ELASTICSEARCHOBJECT]
	SET [RESERVEDBY] = NULL, [RESERVEDDATE] = NULL
	WHERE [PROCESSEDDATE] IS NULL
	AND [RESERVEDDATE] IS NOT NULL
	AND [RESERVEDDATE] < DATEADD(MINUTE, -@TIMEOUT, GETUTCDATE());
END