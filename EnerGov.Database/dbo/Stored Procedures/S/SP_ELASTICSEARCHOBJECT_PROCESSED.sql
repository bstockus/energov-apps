CREATE PROCEDURE [dbo].[SP_ELASTICSEARCHOBJECT_PROCESSED] (
	@PROCESSED_IDS [RecordIDs] READONLY
	,@PROCESSED_DATE DATETIME
) AS
BEGIN
	UPDATE [ELASTICSEARCHOBJECT]
	SET [PROCESSEDDATE] = @PROCESSED_DATE
	WHERE [OBJECTID] IN (
		SELECT [RECORDID] 
		FROM @PROCESSED_IDS
	);
END