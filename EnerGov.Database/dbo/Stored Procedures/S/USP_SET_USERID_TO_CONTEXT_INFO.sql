CREATE PROCEDURE [dbo].[USP_SET_USERID_TO_CONTEXT_INFO]
	@CONTEXTUSERID CHAR(36)
AS
BEGIN
	DECLARE @EncodedUserId VARBINARY(128);
	SET @EncodedUserId = CONVERT(VARBINARY(128), CONVERT(CHAR(36), @CONTEXTUSERID));
	SET CONTEXT_INFO @EncodedUserId;
END