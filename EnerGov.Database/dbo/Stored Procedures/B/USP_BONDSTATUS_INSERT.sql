CREATE PROCEDURE [dbo].[USP_BONDSTATUS_INSERT]
(
	@BONDSTATUSID CHAR(36),
	@NAME NVARCHAR(50),
	@DESCRIPTION NVARCHAR(MAX),
	@SUCCESSFLAG BIT,
	@FAILUREFLAG BIT,
	@CANCELLEDFLAG BIT,
	@LASTCHANGEDBY CHAR(36),
	@LASTCHANGEDON DATETIME,
	@ROWVERSION INT
)
AS
DECLARE @OUTPUTTABLE as TABLE([ROWVERSION]  int)
INSERT INTO [dbo].[BONDSTATUS](
	[BONDSTATUSID],
	[NAME],
	[DESCRIPTION],
	[SUCCESSFLAG],
	[FAILUREFLAG],
	[CANCELLEDFLAG],
	[LASTCHANGEDBY],
	[LASTCHANGEDON],
	[ROWVERSION]
)
OUTPUT inserted.[ROWVERSION] INTO @OUTPUTTABLE
VALUES
(
	@BONDSTATUSID,
	@NAME,
	@DESCRIPTION,
	@SUCCESSFLAG,
	@FAILUREFLAG,
	@CANCELLEDFLAG,
	@LASTCHANGEDBY,
	@LASTCHANGEDON,
	@ROWVERSION
)
SELECT * FROM @OUTPUTTABLE