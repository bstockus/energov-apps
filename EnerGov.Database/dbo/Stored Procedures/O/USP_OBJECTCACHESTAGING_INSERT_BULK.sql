CREATE PROCEDURE [dbo].[USP_OBJECTCACHESTAGING_INSERT_BULK]
	@NAMES [dbo].[LONG_NAMES] READONLY
AS
SET NOCOUNT ON;
INSERT INTO [dbo].[OBJECTCACHESTAGING] (CLASSNAME)
SELECT [NAME] FROM @NAMES AS NEW_DATA
WHERE NOT EXISTS(SELECT * FROM [dbo].[OBJECTCACHESTAGING] WHERE CLASSNAME = NEW_DATA.NAME)