CREATE TABLE [dbo].[MENUBOXITEMS] (
    [FKFORMID]     CHAR (36) NOT NULL,
    [DISPLAYORDER] INT       NOT NULL,
    CONSTRAINT [FK_MENUBOXITEMS_FORMS] FOREIGN KEY ([FKFORMID]) REFERENCES [dbo].[FORMS] ([SFORMSGUID])
);

