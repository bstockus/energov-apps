CREATE TABLE [dbo].[MAILINGADDRESSSTREETTYPE] (
    [MAILINGADDRESSSTREETTYPEID] CHAR (36)      NOT NULL,
    [NAME]                       NVARCHAR (50)  NOT NULL,
    [DESCRIPTION]                NVARCHAR (MAX) NULL,
    [STREETTYPE]                 INT            NOT NULL,
    CONSTRAINT [PK_MailingAddressStreetType] PRIMARY KEY CLUSTERED ([MAILINGADDRESSSTREETTYPEID] ASC) WITH (FILLFACTOR = 80),
    CONSTRAINT [FK_MailAddrStreetType_Type] FOREIGN KEY ([STREETTYPE]) REFERENCES [dbo].[STREETTYPE] ([STREETTYPEID])
);

