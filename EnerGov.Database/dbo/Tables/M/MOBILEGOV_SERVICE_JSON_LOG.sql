CREATE TABLE [dbo].[MOBILEGOV_SERVICE_JSON_LOG] (
    [JSON_ID]   CHAR (36)      NOT NULL,
    [JSON]      NVARCHAR (MAX) NOT NULL,
    [TIMESTAMP] DATETIME       NOT NULL,
    CONSTRAINT [PK_MOBILEGOV_SERVICE_JSON_LOG] PRIMARY KEY CLUSTERED ([JSON_ID] ASC) WITH (FILLFACTOR = 90)
);

