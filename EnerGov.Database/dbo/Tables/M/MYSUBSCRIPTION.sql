CREATE TABLE [dbo].[MYSUBSCRIPTION] (
    [MYSUBSCRIPTIONID] CHAR (36) NOT NULL,
    [PARENTID]         CHAR (36) NOT NULL,
    [USERID]           CHAR (36) NOT NULL,
    [LASTVIEWEDON]     DATETIME  NOT NULL,
    CONSTRAINT [PK_MYSUBSCRIPTION] PRIMARY KEY CLUSTERED ([MYSUBSCRIPTIONID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_MYSUBSCRIPTION_USERS] FOREIGN KEY ([USERID]) REFERENCES [dbo].[USERS] ([SUSERGUID])
);

