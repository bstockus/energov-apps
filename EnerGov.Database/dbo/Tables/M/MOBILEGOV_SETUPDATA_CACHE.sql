CREATE TABLE [dbo].[MOBILEGOV_SETUPDATA_CACHE] (
    [CACHEID]       INT            NOT NULL,
    [SERVERVERSION] NVARCHAR (20)  NOT NULL,
    [SETUPCACHE]    NVARCHAR (MAX) NOT NULL,
    CONSTRAINT [PK_MOBILEGOV_SETUPDATA_CACHE] PRIMARY KEY CLUSTERED ([CACHEID] ASC) WITH (FILLFACTOR = 90)
);

