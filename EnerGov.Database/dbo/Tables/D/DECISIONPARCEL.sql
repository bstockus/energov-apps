CREATE TABLE [dbo].[DECISIONPARCEL] (
    [DECISIONPARCELID] CHAR (36)     NOT NULL,
    [REQUIRED]         BIT           NULL,
    [INSTRUCTIONS]     VARCHAR (MAX) NULL,
    CONSTRAINT [PK_DECISIONPARCEL] PRIMARY KEY CLUSTERED ([DECISIONPARCELID] ASC) WITH (FILLFACTOR = 90)
);

