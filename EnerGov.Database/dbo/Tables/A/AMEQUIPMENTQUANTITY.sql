CREATE TABLE [dbo].[AMEQUIPMENTQUANTITY] (
    [AMEQUIPMENTQUANTITYID] CHAR (36) NOT NULL,
    [AMEQUIPMENTTYPEID]     CHAR (36) NOT NULL,
    [QUANTITY]              INT       NOT NULL,
    [INSTOREID]             CHAR (36) NOT NULL,
    CONSTRAINT [PK_AMEquipmentQty] PRIMARY KEY CLUSTERED ([AMEQUIPMENTQUANTITYID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_AMEquipmentQty_AMEquipmentT] FOREIGN KEY ([AMEQUIPMENTTYPEID]) REFERENCES [dbo].[AMEQUIPMENTTYPE] ([AMEQUIPMENTTYPEID]),
    CONSTRAINT [FK_AMEquipmentQty_INStore] FOREIGN KEY ([INSTOREID]) REFERENCES [dbo].[INSTORE] ([INSTOREID])
);

