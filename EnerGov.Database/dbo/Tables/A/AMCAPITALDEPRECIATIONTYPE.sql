CREATE TABLE [dbo].[AMCAPITALDEPRECIATIONTYPE] (
    [AMCAPITALDEPRECIATIONTYPEID] CHAR (36)      NOT NULL,
    [NAME]                        VARCHAR (100)  NOT NULL,
    [DESCRIPTION]                 NVARCHAR (MAX) NULL,
    [SYSTEMACTIONID]              CHAR (36)      NOT NULL,
    CONSTRAINT [PK_AMCapitalDepreciationType] PRIMARY KEY CLUSTERED ([AMCAPITALDEPRECIATIONTYPEID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_AMCapitalDepreciationType_SystemAction] FOREIGN KEY ([SYSTEMACTIONID]) REFERENCES [dbo].[SYSTEMACTION] ([SYSTEMACTIONID])
);

