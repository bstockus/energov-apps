CREATE TABLE [dbo].[FORMFIELDS] (
    [FORMFIELDID]  INT            NOT NULL,
    [SCOMMONNAME]  NVARCHAR (100) NOT NULL,
    [FKSFORMSGUID] CHAR (36)      NOT NULL,
    CONSTRAINT [PK_FormFieldSecurity] PRIMARY KEY CLUSTERED ([FORMFIELDID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_FormFieldSecurity_Forms] FOREIGN KEY ([FKSFORMSGUID]) REFERENCES [dbo].[FORMS] ([SFORMSGUID])
);

