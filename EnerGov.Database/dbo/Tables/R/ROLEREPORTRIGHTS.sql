CREATE TABLE [dbo].[ROLEREPORTRIGHTS] (
    [SREPORTID] CHAR (36) NOT NULL,
    [SROLEGUID] CHAR (36) NOT NULL,
    [BVISIBLE]  BIT       CONSTRAINT [DF_ReportRights_bVisible] DEFAULT ((1)) NOT NULL,
    CONSTRAINT [PK_ReportRights] PRIMARY KEY CLUSTERED ([SREPORTID] ASC, [SROLEGUID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_RoleReportRights_Roles] FOREIGN KEY ([SROLEGUID]) REFERENCES [dbo].[ROLES] ([SROLEGUID])
);

