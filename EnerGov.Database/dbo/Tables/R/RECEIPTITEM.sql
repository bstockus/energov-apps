CREATE TABLE [dbo].[RECEIPTITEM] (
    [RECEIPTITEMID] CHAR (36)       NOT NULL,
    [RECEIPTID]     CHAR (36)       NOT NULL,
    [ITEMID]        CHAR (36)       NOT NULL,
    [AMOUNT]        DECIMAL (18, 4) NULL,
    [PROCESSEDDATE] DATETIME        NULL,
    [ERRORMESSAGE]  NVARCHAR (MAX)  NULL,
    CONSTRAINT [PK_RECEIPTITEM] PRIMARY KEY CLUSTERED ([RECEIPTITEMID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_RECEIPTITEM_RECEIPT] FOREIGN KEY ([RECEIPTID]) REFERENCES [dbo].[RECEIPT] ([RECEIPTID])
);

