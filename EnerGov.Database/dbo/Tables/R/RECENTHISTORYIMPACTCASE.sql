CREATE TABLE [dbo].[RECENTHISTORYIMPACTCASE] (
    [RECENTHISTORYIMPACTCASEID] CHAR (36)     NOT NULL,
    [IMPACTCASEID]              CHAR (36)     NOT NULL,
    [LOGGEDDATETIME]            DATETIME      NOT NULL,
    [USERID]                    CHAR (36)     NOT NULL,
    [CASENUMBER]                NVARCHAR (50) NULL,
    CONSTRAINT [PK_RECENTHISTORYIMPACTCASE] PRIMARY KEY CLUSTERED ([RECENTHISTORYIMPACTCASEID] ASC),
    CONSTRAINT [FK_RECENTHISTORYIMPACTCASE_USERS] FOREIGN KEY ([USERID]) REFERENCES [dbo].[USERS] ([SUSERGUID])
);


GO
CREATE NONCLUSTERED INDEX [IX_RECENTHISTORYIMPACTCASE_ALL]
    ON [dbo].[RECENTHISTORYIMPACTCASE]([USERID] ASC, [CASENUMBER] DESC, [IMPACTCASEID] ASC);


GO
CREATE NONCLUSTERED INDEX [IX_RECENTHISTORYIMPACTCASE_LOOKUP]
    ON [dbo].[RECENTHISTORYIMPACTCASE]([IMPACTCASEID] ASC, [USERID] ASC, [CASENUMBER] DESC);

