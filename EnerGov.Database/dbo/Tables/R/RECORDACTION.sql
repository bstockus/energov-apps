CREATE TABLE [dbo].[RECORDACTION] (
    [RECORDACTIONID] INT           NOT NULL,
    [NAME]           NVARCHAR (50) NOT NULL,
    CONSTRAINT [PK_RECORDACTION] PRIMARY KEY CLUSTERED ([RECORDACTIONID] ASC)
);

