CREATE TABLE [dbo].[BCONLINE] (
    [BCONLINELICENSENUM] NVARCHAR (10) NOT NULL,
    [ENERGOVLICENSENUM]  NVARCHAR (50) NOT NULL,
    [ENERGOVLICENSETYPE] NVARCHAR (10) NOT NULL,
    [GLOBALENTITYID]     CHAR (36)     NOT NULL,
    [SUSERGUID]          CHAR (36)     NOT NULL,
    [CREATEDATE]         DATETIME      NOT NULL,
    CONSTRAINT [PK_BCONLINE] PRIMARY KEY CLUSTERED ([BCONLINELICENSENUM] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_BCONLINE_GLENTITY] FOREIGN KEY ([GLOBALENTITYID]) REFERENCES [dbo].[GLOBALENTITY] ([GLOBALENTITYID]),
    CONSTRAINT [FK_BCONLINE_USERS] FOREIGN KEY ([SUSERGUID]) REFERENCES [dbo].[USERS] ([SUSERGUID])
);

