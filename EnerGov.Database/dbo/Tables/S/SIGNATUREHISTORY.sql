CREATE TABLE [dbo].[SIGNATUREHISTORY] (
    [PMPERMITID]     CHAR (36)       NULL,
    [SIGNATUREVALUE] VARBINARY (MAX) NOT NULL,
    [GLOBALENTITYID] CHAR (36)       NOT NULL,
    [PLPLANID]       CHAR (36)       NULL,
    [SIGNDATE]       DATETIME        CONSTRAINT [DF_SignatureHistory_SignDate] DEFAULT (getdate()) NOT NULL,
    [SIGNATUREID]    CHAR (36)       CONSTRAINT [DF_SignatureHistory_SignatureID] DEFAULT (newid()) NOT NULL,
    CONSTRAINT [PK_SignatureHistory] PRIMARY KEY CLUSTERED ([SIGNATUREID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_Signature_Entity] FOREIGN KEY ([GLOBALENTITYID]) REFERENCES [dbo].[GLOBALENTITY] ([GLOBALENTITYID]),
    CONSTRAINT [FK_Signature_Permit] FOREIGN KEY ([PMPERMITID]) REFERENCES [dbo].[PMPERMIT] ([PMPERMITID]),
    CONSTRAINT [FK_Signature_Plan] FOREIGN KEY ([PLPLANID]) REFERENCES [dbo].[PLPLAN] ([PLPLANID])
);

