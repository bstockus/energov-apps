CREATE TABLE [dbo].[SYSTEMACTION] (
    [SYSTEMACTIONID] CHAR (36)      NOT NULL,
    [NAME]           VARCHAR (50)   NOT NULL,
    [DESCRIPTION]    NVARCHAR (MAX) NULL,
    [OBJECT]         VARCHAR (100)  NOT NULL,
    CONSTRAINT [PK_SystemAction] PRIMARY KEY CLUSTERED ([SYSTEMACTIONID] ASC) WITH (FILLFACTOR = 90)
);

