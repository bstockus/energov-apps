CREATE TABLE [dbo].[CAPAYMENTTYPE] (
    [CAPAYMENTTYPEID] INT            NOT NULL,
    [NAME]            NVARCHAR (50)  NOT NULL,
    [DESCRIPTION]     NVARCHAR (MAX) NULL,
    CONSTRAINT [PK_CAPaymentType] PRIMARY KEY CLUSTERED ([CAPAYMENTTYPEID] ASC) WITH (FILLFACTOR = 90)
);

