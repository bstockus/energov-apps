CREATE TABLE [dbo].[CMCITATION] (
    [CMCITATIONID]       CHAR (36)      NOT NULL,
    [CITATIONNUMBER]     NVARCHAR (50)  NOT NULL,
    [DESCRIPTION]        NVARCHAR (MAX) NULL,
    [ISSUEDATE]          DATETIME       NOT NULL,
    [CMCODECASEID]       CHAR (36)      NOT NULL,
    [CMCITATIONTYPEID]   CHAR (36)      NOT NULL,
    [CMCITATIONSTATUSID] CHAR (36)      NOT NULL,
    CONSTRAINT [PK_CMCITATION_CMCITATIONID] PRIMARY KEY CLUSTERED ([CMCITATIONID] ASC),
    CONSTRAINT [FK_CMCITATION_CMCITATIONSTATUSID] FOREIGN KEY ([CMCITATIONSTATUSID]) REFERENCES [dbo].[CMCITATIONSTATUS] ([CMCITATIONSTATUSID]),
    CONSTRAINT [FK_CMCITATION_CMCITATIONTYPEID] FOREIGN KEY ([CMCITATIONTYPEID]) REFERENCES [dbo].[CMCITATIONTYPE] ([CMCITATIONTYPEID]),
    CONSTRAINT [FK_CMCITATION_CMCODECASEID] FOREIGN KEY ([CMCODECASEID]) REFERENCES [dbo].[CMCODECASE] ([CMCODECASEID]),
    CONSTRAINT [UC_CMCITATION_CITATIONNUMBER] UNIQUE NONCLUSTERED ([CITATIONNUMBER] ASC)
);


GO
CREATE NONCLUSTERED INDEX [IX_CMCITATION_CMCODECASEID_CMCITATIONTYPEID]
    ON [dbo].[CMCITATION]([CMCODECASEID] ASC, [CMCITATIONTYPEID] ASC);

