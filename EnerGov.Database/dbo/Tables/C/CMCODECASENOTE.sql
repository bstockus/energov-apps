CREATE TABLE [dbo].[CMCODECASENOTE] (
    [CMCODECASENOTEID] CHAR (36)      NOT NULL,
    [CMCODECASEID]     CHAR (36)      NOT NULL,
    [TEXT]             NVARCHAR (MAX) NOT NULL,
    [CREATEDBY]        CHAR (36)      NULL,
    [CREATEDDATE]      DATETIME       NULL,
    [TITLE]            NVARCHAR (50)  CONSTRAINT [DF_CMCODECASENOTE_TITLE] DEFAULT ('') NULL,
    [PIN]              BIT            CONSTRAINT [DF_CMCODECASENOTE_PIN] DEFAULT ((0)) NOT NULL,
    CONSTRAINT [PK_CMCodeCaseNote] PRIMARY KEY CLUSTERED ([CMCODECASENOTEID] ASC) WITH (FILLFACTOR = 80),
    CONSTRAINT [FK_CaseNote_CodeCase] FOREIGN KEY ([CMCODECASEID]) REFERENCES [dbo].[CMCODECASE] ([CMCODECASEID]),
    CONSTRAINT [FK_CaseNote_Users] FOREIGN KEY ([CREATEDBY]) REFERENCES [dbo].[USERS] ([SUSERGUID])
);

