CREATE TABLE [dbo].[CMCODECASEACTIONREF] (
    [CMCODECASEACTIONREFID] CHAR (36)      NOT NULL,
    [CMCODECASEID]          CHAR (36)      NOT NULL,
    [CMCODECASEWFMSG]       NVARCHAR (200) NULL,
    [WFENTITYID]            INT            NOT NULL,
    [PARENTCASEID]          CHAR (36)      NULL,
    CONSTRAINT [PK_CMCODECASEACTIONREF] PRIMARY KEY NONCLUSTERED ([CMCODECASEACTIONREFID] ASC) WITH (FILLFACTOR = 80),
    CONSTRAINT [FK_CMACTIONREF_CMID] FOREIGN KEY ([CMCODECASEID]) REFERENCES [dbo].[CMCODECASE] ([CMCODECASEID]),
    CONSTRAINT [FK_CMACTIONREF_WFENTITY] FOREIGN KEY ([WFENTITYID]) REFERENCES [dbo].[WFENTITY] ([WFENTITYID])
);

