CREATE TABLE [dbo].[CAFEEDELETION] (
    [CAFEEDELETIONID]     CHAR (36)      NOT NULL,
    [CACOMPUTEDFEEID]     CHAR (36)      NOT NULL,
    [CAFEEDELETEREASONID] CHAR (36)      NOT NULL,
    [USERID]              CHAR (36)      NOT NULL,
    [DELETIONDATE]        DATETIME       NOT NULL,
    [NOTES]               NVARCHAR (MAX) NULL,
    CONSTRAINT [PK_CAFeeDeletion] PRIMARY KEY CLUSTERED ([CAFEEDELETIONID] ASC) WITH (FILLFACTOR = 80),
    CONSTRAINT [FK_CAFeeDeletion_Reason] FOREIGN KEY ([CAFEEDELETEREASONID]) REFERENCES [dbo].[CAFEEDELETEREASON] ([CAFEEDELETEREASONID]),
    CONSTRAINT [FK_FeeDeletion_Fee] FOREIGN KEY ([CACOMPUTEDFEEID]) REFERENCES [dbo].[CACOMPUTEDFEE] ([CACOMPUTEDFEEID]),
    CONSTRAINT [FK_FeeDeletion_User] FOREIGN KEY ([USERID]) REFERENCES [dbo].[USERS] ([SUSERGUID])
);

