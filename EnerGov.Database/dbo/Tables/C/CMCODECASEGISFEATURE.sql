CREATE TABLE [dbo].[CMCODECASEGISFEATURE] (
    [CMCODECASEGISFEATUREID] CHAR (36)     NOT NULL,
    [FEATURELAYER]           VARCHAR (100) NOT NULL,
    [ATTRIBUTEFIELD]         VARCHAR (50)  NULL,
    [KEYVALUE]               VARCHAR (50)  NOT NULL,
    [CMCODECASEID]           CHAR (36)     NOT NULL,
    CONSTRAINT [PK_CMCodeCaseGISFeatures] PRIMARY KEY CLUSTERED ([CMCODECASEGISFEATUREID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_GISFeature_CodeCase] FOREIGN KEY ([CMCODECASEID]) REFERENCES [dbo].[CMCODECASE] ([CMCODECASEID])
);

