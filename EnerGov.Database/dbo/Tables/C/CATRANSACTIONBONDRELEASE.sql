CREATE TABLE [dbo].[CATRANSACTIONBONDRELEASE] (
    [CATRANSACTIONBONDRELEASEID] CHAR (36) NOT NULL,
    [CATRANSACTIONID]            CHAR (36) NOT NULL,
    [BONDRELEASEID]              CHAR (36) NOT NULL,
    [GLOBALENTITYACCOUNTID]      CHAR (36) NOT NULL,
    CONSTRAINT [PK_CATRAN_BONDRELEASE] PRIMARY KEY NONCLUSTERED ([CATRANSACTIONBONDRELEASEID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_BONDREL_ACCOUNT] FOREIGN KEY ([GLOBALENTITYACCOUNTID]) REFERENCES [dbo].[GLOBALENTITYACCOUNT] ([GLOBALENTITYACCOUNTID]),
    CONSTRAINT [FK_BONDREL_BOND] FOREIGN KEY ([BONDRELEASEID]) REFERENCES [dbo].[BONDRELEASE] ([BONDRELEASEID]),
    CONSTRAINT [FK_BONDREL_CATRAN] FOREIGN KEY ([CATRANSACTIONID]) REFERENCES [dbo].[CATRANSACTION] ([CATRANSACTIONID])
);

