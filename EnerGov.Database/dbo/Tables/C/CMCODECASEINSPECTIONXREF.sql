CREATE TABLE [dbo].[CMCODECASEINSPECTIONXREF] (
    [CMCODECASEINSPECTIONXREFID] CHAR (36) NOT NULL,
    [CMCODECASEID]               CHAR (36) NOT NULL,
    [IMINSPECTIONID]             CHAR (36) NOT NULL,
    CONSTRAINT [PK_CMCODECASEINSPECTIONXREF] PRIMARY KEY CLUSTERED ([CMCODECASEINSPECTIONXREFID] ASC),
    CONSTRAINT [FK_CMCODECASEINSPECTIONXREF_CM] FOREIGN KEY ([CMCODECASEID]) REFERENCES [dbo].[CMCODECASE] ([CMCODECASEID]),
    CONSTRAINT [FK_CMCODECASEINSPECTIONXREF_IM] FOREIGN KEY ([IMINSPECTIONID]) REFERENCES [dbo].[IMINSPECTION] ([IMINSPECTIONID])
);

