CREATE TABLE [dbo].[CATILLGROUP] (
    [TILLGROUPID]    CHAR (36)    NOT NULL,
    [GROUPNUMBER]    VARCHAR (50) NOT NULL,
    [TIISESSIONDATE] DATETIME     NULL,
    [GROUPBALANCE]   MONEY        NULL,
    [CLOSEDBY]       CHAR (36)    NULL,
    CONSTRAINT [PK_CATILLGROUP] PRIMARY KEY CLUSTERED ([TILLGROUPID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_CATILLGROUP_CLOSED] FOREIGN KEY ([CLOSEDBY]) REFERENCES [dbo].[USERS] ([SUSERGUID])
);

