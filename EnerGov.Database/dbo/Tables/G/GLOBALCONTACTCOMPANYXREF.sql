CREATE TABLE [dbo].[GLOBALCONTACTCOMPANYXREF] (
    [GLOBALCONTACTCOMPANYXREFID] CHAR (36) NOT NULL,
    [CONTACTID]                  CHAR (36) NOT NULL,
    [COMPANYID]                  CHAR (36) NOT NULL,
    CONSTRAINT [PK_GlobalContactCompanyXRef] PRIMARY KEY CLUSTERED ([GLOBALCONTACTCOMPANYXREFID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_ContactCompanyXR_Contact] FOREIGN KEY ([CONTACTID]) REFERENCES [dbo].[CONTACT] ([CONTACTID]),
    CONSTRAINT [FK_GlobalContactCompanyXRef_Co] FOREIGN KEY ([COMPANYID]) REFERENCES [dbo].[COMPANY] ([COMPANYID])
);

