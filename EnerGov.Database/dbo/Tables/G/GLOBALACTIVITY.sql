CREATE TABLE [dbo].[GLOBALACTIVITY] (
    [GLOBALACTIVITYID]     CHAR (36)      NOT NULL,
    [GLOBALPARENTID]       CHAR (36)      NOT NULL,
    [GLOBALACTIVITYTYPEID] CHAR (36)      NOT NULL,
    [ACTIVITYNAME]         NVARCHAR (255) NOT NULL,
    [ACTIVITYNUMBER]       NVARCHAR (255) NOT NULL,
    [ACTIVITYCOMMENTS]     NVARCHAR (MAX) NOT NULL,
    [SUSERGUID]            CHAR (36)      NOT NULL,
    [CREATEDON]            DATETIME       NOT NULL,
    CONSTRAINT [PK_GlobalActivity] PRIMARY KEY CLUSTERED ([GLOBALACTIVITYID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_GlobalActivity_GlobalActivityType] FOREIGN KEY ([GLOBALACTIVITYTYPEID]) REFERENCES [dbo].[GLOBALACTIVITYTYPE] ([GLOBALACTIVITYTYPEID]),
    CONSTRAINT [FK_GlobalActivity_Users] FOREIGN KEY ([SUSERGUID]) REFERENCES [dbo].[USERS] ([SUSERGUID]),
    CONSTRAINT [FK_GlobalActivity_Users1] FOREIGN KEY ([SUSERGUID]) REFERENCES [dbo].[USERS] ([SUSERGUID])
);

