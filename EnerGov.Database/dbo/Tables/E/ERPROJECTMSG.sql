CREATE TABLE [dbo].[ERPROJECTMSG] (
    [ERPROJECTMSGID] CHAR (36)     NOT NULL,
    [ERPROJECT]      CHAR (36)     NOT NULL,
    [MESSAGE]        VARCHAR (200) NULL,
    [ROWVERSION]     INT           NOT NULL,
    CONSTRAINT [PK_ERProjectMsg] PRIMARY KEY CLUSTERED ([ERPROJECTMSGID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_ERProjectMsg_ERProject] FOREIGN KEY ([ERPROJECT]) REFERENCES [dbo].[ERPROJECT] ([ERPROJECTID])
);

