CREATE TABLE [dbo].[EXAMFORMAT] (
    [EXAMFORMATID] INT           NOT NULL,
    [NAME]         NVARCHAR (50) NOT NULL,
    CONSTRAINT [PK_EXAMFORMAT] PRIMARY KEY CLUSTERED ([EXAMFORMATID] ASC) WITH (FILLFACTOR = 90)
);

