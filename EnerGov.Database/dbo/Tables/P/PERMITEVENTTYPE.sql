CREATE TABLE [dbo].[PERMITEVENTTYPE] (
    [PERMITEVENTTYPEID] INT            NOT NULL,
    [NAME]              NVARCHAR (50)  NOT NULL,
    [DESCRIPTION]       NVARCHAR (MAX) NULL,
    [ACTIVE]            BIT            CONSTRAINT [DF_PermitEventType_Active] DEFAULT ((1)) NOT NULL,
    CONSTRAINT [PK_PERMITEVENTTYPE] PRIMARY KEY CLUSTERED ([PERMITEVENTTYPEID] ASC)
);

