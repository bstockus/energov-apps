CREATE TABLE [dbo].[PLPLANBOND] (
    [PLPLANBONDID] CHAR (36) NOT NULL,
    [PLPLANID]     CHAR (36) NOT NULL,
    [BONDID]       CHAR (36) NOT NULL,
    [CREATEDBY]    CHAR (36) NULL,
    [CREATEDATE]   DATETIME  NULL,
    CONSTRAINT [PK_PLPlanBond] PRIMARY KEY CLUSTERED ([PLPLANBONDID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_PLPlanBond_Bond] FOREIGN KEY ([BONDID]) REFERENCES [dbo].[BOND] ([BONDID]),
    CONSTRAINT [FK_PLPlanBond_PLPlan] FOREIGN KEY ([PLPLANID]) REFERENCES [dbo].[PLPLAN] ([PLPLANID]),
    CONSTRAINT [FK_PLPlanBond_Users] FOREIGN KEY ([CREATEDBY]) REFERENCES [dbo].[USERS] ([SUSERGUID])
);


GO
CREATE NONCLUSTERED INDEX [NCIDX1_PLPLANBOND_BONDID]
    ON [dbo].[PLPLANBOND]([BONDID] ASC)
    INCLUDE([PLPLANID]);

