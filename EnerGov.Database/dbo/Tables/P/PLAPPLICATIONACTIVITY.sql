CREATE TABLE [dbo].[PLAPPLICATIONACTIVITY] (
    [PLAPPLICATIONACTIVITYID]     CHAR (36)      NOT NULL,
    [PLAPPLICATIONID]             CHAR (36)      NOT NULL,
    [PLAPPLICATIONACTIVITYTYPEID] CHAR (36)      NOT NULL,
    [APPLICATIONACTIVITYNAME]     NVARCHAR (255) NOT NULL,
    [APPLICATIONACTIVITYNUMBER]   NVARCHAR (255) NOT NULL,
    [APPLICATIONACTIVITYCOMMENTS] NVARCHAR (MAX) NOT NULL,
    [SUSERGUID]                   CHAR (36)      NOT NULL,
    [CREATEDON]                   DATETIME       NOT NULL,
    CONSTRAINT [PK_PLApplicationActivity] PRIMARY KEY CLUSTERED ([PLAPPLICATIONACTIVITYID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_PLApplicationActivity_PLApplication] FOREIGN KEY ([PLAPPLICATIONID]) REFERENCES [dbo].[PLAPPLICATION] ([PLAPPLICATIONID]),
    CONSTRAINT [FK_PLApplicationActivity_PLApplicationActivityType] FOREIGN KEY ([PLAPPLICATIONACTIVITYTYPEID]) REFERENCES [dbo].[PLAPPLICATIONACTIVITYTYPE] ([PLAPPLICATIONACTIVITYTYPEID]),
    CONSTRAINT [FK_PLApplicationActivity_Users] FOREIGN KEY ([SUSERGUID]) REFERENCES [dbo].[USERS] ([SUSERGUID])
);

