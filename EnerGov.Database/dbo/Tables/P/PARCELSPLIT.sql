CREATE TABLE [dbo].[PARCELSPLIT] (
    [PARCELSPLITID] CHAR (36)     NOT NULL,
    [PARCELID]      CHAR (36)     NOT NULL,
    [MODULE]        NVARCHAR (50) NOT NULL,
    [CASEID]        CHAR (36)     NOT NULL,
    [CASENUMBER]    NVARCHAR (50) NOT NULL,
    [CASETYPE]      CHAR (36)     NOT NULL,
    [PROCESSED]     BIT           CONSTRAINT [DF_ParcelSplit_Processed] DEFAULT ((0)) NOT NULL,
    CONSTRAINT [PK_ParcelSplit] PRIMARY KEY CLUSTERED ([PARCELSPLITID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_PARCELSPLIT_PARCEL1] FOREIGN KEY ([PARCELID]) REFERENCES [dbo].[PARCEL] ([PARCELID])
);

