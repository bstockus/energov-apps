CREATE TABLE [dbo].[PLAPPLICATIONCONTACT] (
    [PLAPPLICATIONCONTACTID] CHAR (36) NOT NULL,
    [PLAPPLICATIONID]        CHAR (36) NOT NULL,
    [PLPLANCONTACTTYPEID]    CHAR (36) NOT NULL,
    [GLOBALENTITYID]         CHAR (36) NOT NULL,
    [ISBILLING]              BIT       CONSTRAINT [DF_PLApplicationContact_Billing] DEFAULT ((0)) NOT NULL,
    CONSTRAINT [PK_PLApplicationContact] PRIMARY KEY CLUSTERED ([PLAPPLICATIONCONTACTID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_PLApplicationContact_GlobalEntity] FOREIGN KEY ([GLOBALENTITYID]) REFERENCES [dbo].[GLOBALENTITY] ([GLOBALENTITYID]),
    CONSTRAINT [FK_PLApplicationContact_PLApplication] FOREIGN KEY ([PLAPPLICATIONID]) REFERENCES [dbo].[PLAPPLICATION] ([PLAPPLICATIONID])
);


GO
CREATE NONCLUSTERED INDEX [IX_PLAPPLICATIONCONTACT_GLOBALENTITY]
    ON [dbo].[PLAPPLICATIONCONTACT]([GLOBALENTITYID] ASC);

