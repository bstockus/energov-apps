CREATE TABLE [dbo].[PLPLANGISFEATURE] (
    [PLPLANGISFEATUREID] CHAR (36)     NOT NULL,
    [FEATURELAYER]       VARCHAR (100) NOT NULL,
    [ATTRIBUTEFIELD]     VARCHAR (50)  NULL,
    [KEYVALUE]           VARCHAR (50)  NOT NULL,
    [PLPLANID]           CHAR (36)     NOT NULL,
    CONSTRAINT [PK_PLPlanGISFeatures] PRIMARY KEY CLUSTERED ([PLPLANGISFEATUREID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_PLPlanGISFeature_PLPlan] FOREIGN KEY ([PLPLANID]) REFERENCES [dbo].[PLPLAN] ([PLPLANID])
);


GO
CREATE NONCLUSTERED INDEX [IX_PLNGISFEAT_PERMIT_ALL]
    ON [dbo].[PLPLANGISFEATURE]([PLPLANID] ASC)
    INCLUDE([ATTRIBUTEFIELD], [FEATURELAYER], [KEYVALUE]) WITH (FILLFACTOR = 90);

