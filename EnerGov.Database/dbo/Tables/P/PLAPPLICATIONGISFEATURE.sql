CREATE TABLE [dbo].[PLAPPLICATIONGISFEATURE] (
    [APPLICATIONGISFEATUREID] CHAR (36)     NOT NULL,
    [FEATURELAYER]            VARCHAR (100) NOT NULL,
    [ATTRIBUTEFIELD]          VARCHAR (50)  NULL,
    [KEYVALUE]                VARCHAR (50)  NOT NULL,
    [PLAPPLICATIONID]         CHAR (36)     NOT NULL,
    CONSTRAINT [PK_PLApplicationGISFeature] PRIMARY KEY CLUSTERED ([APPLICATIONGISFEATUREID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_AppGISFeature_PLApp] FOREIGN KEY ([PLAPPLICATIONID]) REFERENCES [dbo].[PLAPPLICATION] ([PLAPPLICATIONID])
);

