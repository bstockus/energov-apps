CREATE TABLE [dbo].[PMPERMITACTIVITYTAB] (
    [PMPERMITACTIVITYTABID]  CHAR (36)     NOT NULL,
    [PMPERMITACTIVITYTYPEID] CHAR (36)     NOT NULL,
    [NAME]                   NVARCHAR (50) NOT NULL,
    [PRIORITYORDER]          INT           NULL,
    CONSTRAINT [PK_PMPermitActivityTab] PRIMARY KEY CLUSTERED ([PMPERMITACTIVITYTABID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_PMActivityTab_ActType] FOREIGN KEY ([PMPERMITACTIVITYTYPEID]) REFERENCES [dbo].[PMPERMITACTIVITYTYPE] ([PMPERMITACTIVITYTYPEID])
);

