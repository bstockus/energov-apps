CREATE TABLE [dbo].[PLPLANNOTE] (
    [PLPLANNOTEID] CHAR (36)      NOT NULL,
    [PLPLANID]     CHAR (36)      NOT NULL,
    [TEXT]         NVARCHAR (MAX) NULL,
    [CREATEDBY]    CHAR (36)      NULL,
    [CREATEDDATE]  DATETIME       NULL,
    [TITLE]        NVARCHAR (50)  CONSTRAINT [DF_PLPLANNOTE_TITLE] DEFAULT ('') NULL,
    [PIN]          BIT            CONSTRAINT [DF_PLPLANNOTE_PIN] DEFAULT ((0)) NOT NULL,
    CONSTRAINT [PK_PLPlanNote] PRIMARY KEY CLUSTERED ([PLPLANNOTEID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_PLPlanNote_PLPlan] FOREIGN KEY ([PLPLANID]) REFERENCES [dbo].[PLPLAN] ([PLPLANID]),
    CONSTRAINT [FK_PLPlanNote_Users] FOREIGN KEY ([CREATEDBY]) REFERENCES [dbo].[USERS] ([SUSERGUID])
);

