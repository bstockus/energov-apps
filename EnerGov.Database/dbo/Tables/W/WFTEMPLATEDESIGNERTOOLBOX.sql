CREATE TABLE [dbo].[WFTEMPLATEDESIGNERTOOLBOX] (
    [WFTEMPLATEDESIGNERTOOLBOXID] CHAR (36)      NOT NULL,
    [PROCESSAREA]                 NVARCHAR (MAX) NOT NULL,
    [TOOLBOXDATA]                 NVARCHAR (MAX) NOT NULL,
    [CREATEDON]                   DATETIME       NOT NULL,
    [CREATEDBY]                   CHAR (36)      NOT NULL,
    [LASTCHANGEDON]               DATETIME       NOT NULL,
    [LASTCHANGEDBY]               CHAR (36)      NOT NULL,
    CONSTRAINT [PK_WFTEMPLATEDESIGNERTOOLBOX] PRIMARY KEY CLUSTERED ([WFTEMPLATEDESIGNERTOOLBOXID] ASC),
    CONSTRAINT [FK_WFTEMPLATEDESIGNTOOL_CHANGE] FOREIGN KEY ([LASTCHANGEDBY]) REFERENCES [dbo].[USERS] ([SUSERGUID]),
    CONSTRAINT [FK_WFTEMPLATEDESIGNTOOL_CREATE] FOREIGN KEY ([CREATEDBY]) REFERENCES [dbo].[USERS] ([SUSERGUID])
);

