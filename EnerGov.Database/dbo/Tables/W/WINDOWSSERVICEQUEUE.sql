CREATE TABLE [dbo].[WINDOWSSERVICEQUEUE] (
    [ENTITYID]    CHAR (36) NOT NULL,
    [SERVICETASK] INT       NOT NULL,
    CONSTRAINT [PK_WINDOWSSERVICEQUEUE] PRIMARY KEY CLUSTERED ([ENTITYID] ASC, [SERVICETASK] ASC)
);

