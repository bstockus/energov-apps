CREATE TABLE [dbo].[WFTEMPLATEDESIGNERTEMPLATE] (
    [WFTEMPLATEDESIGNERTEMPLATEID] CHAR (36)      NOT NULL,
    [NAME]                         NVARCHAR (MAX) NOT NULL,
    [DESCRIPTION]                  NVARCHAR (MAX) NOT NULL,
    [PROCESSAREA]                  NVARCHAR (MAX) NOT NULL,
    [TEMPLATEDATA]                 NVARCHAR (MAX) NOT NULL,
    [DEFAULTGROUPNAME]             NVARCHAR (MAX) NOT NULL,
    [CREATEDON]                    DATETIME       NOT NULL,
    [CREATEDBY]                    CHAR (36)      NOT NULL,
    [LASTCHANGEDON]                DATETIME       NOT NULL,
    [LASTCHANGEDBY]                CHAR (36)      NOT NULL,
    CONSTRAINT [PK_WFTEMPLATEDESIGNERTEMPLATE] PRIMARY KEY CLUSTERED ([WFTEMPLATEDESIGNERTEMPLATEID] ASC),
    CONSTRAINT [FK_WFTEMPLATEDESIGNTMPL_CHANGE] FOREIGN KEY ([LASTCHANGEDBY]) REFERENCES [dbo].[USERS] ([SUSERGUID]),
    CONSTRAINT [FK_WFTEMPLATEDESIGNTMPL_CREATE] FOREIGN KEY ([CREATEDBY]) REFERENCES [dbo].[USERS] ([SUSERGUID])
);

