CREATE TABLE [dbo].[INVALIDEMAILQUEUE] (
    [INVALIDEMAILQUEUEID] CHAR (36)      NOT NULL,
    [TO]                  NVARCHAR (MAX) NULL,
    [CC]                  NVARCHAR (MAX) NULL,
    [BCC]                 NVARCHAR (MAX) NULL,
    [EMAILQUEUEID]        CHAR (36)      NOT NULL,
    [COMMENTS]            NVARCHAR (MAX) NULL,
    [CREATEDATE]          DATETIME       NOT NULL,
    CONSTRAINT [PK_INVALIDEMAILQUEUE] PRIMARY KEY CLUSTERED ([INVALIDEMAILQUEUEID] ASC),
    CONSTRAINT [FK_EMAILQUEUE_ID] FOREIGN KEY ([EMAILQUEUEID]) REFERENCES [dbo].[EMAILQUEUE] ([ID]) ON DELETE CASCADE
);


GO
CREATE NONCLUSTERED INDEX [IX_INVALIDEMAILQUEUE_EMAILQUEUEID]
    ON [dbo].[INVALIDEMAILQUEUE]([EMAILQUEUEID] ASC);

