CREATE TABLE [dbo].[IVRACTIONCODE] (
    [IVRACTIONCODE] INT           NOT NULL,
    [NAME]          NVARCHAR (50) NOT NULL,
    CONSTRAINT [PK_IVRActionCode] PRIMARY KEY CLUSTERED ([IVRACTIONCODE] ASC) WITH (FILLFACTOR = 90)
);

