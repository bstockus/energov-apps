CREATE TABLE [dbo].[IPCASEPLANXREF] (
    [IPCASEPLANXREFID] CHAR (36)      NOT NULL,
    [IPCASEID]         CHAR (36)      NOT NULL,
    [PLPLANID]         CHAR (36)      NOT NULL,
    [PARCELNUMBER]     NVARCHAR (255) NULL,
    CONSTRAINT [PK_IPCASEPLANXREF] PRIMARY KEY CLUSTERED ([IPCASEPLANXREFID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_IPCASEPLANXREF_IPCASE] FOREIGN KEY ([IPCASEID]) REFERENCES [dbo].[IPCASE] ([IPCASEID]),
    CONSTRAINT [FK_IPCASEPLANXREF_PLPLAN] FOREIGN KEY ([PLPLANID]) REFERENCES [dbo].[PLPLAN] ([PLPLANID])
);

