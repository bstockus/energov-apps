CREATE TABLE [dbo].[IPCASEPARCEL] (
    [IPCASEPARCELID] CHAR (36) NOT NULL,
    [PARCELID]       CHAR (36) NOT NULL,
    [IPCASEID]       CHAR (36) NOT NULL,
    [MAIN]           BIT       NOT NULL,
    CONSTRAINT [PK_IPCASEPARCEL] PRIMARY KEY CLUSTERED ([IPCASEPARCELID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_IPCASEPARCEL_IPCASE] FOREIGN KEY ([IPCASEID]) REFERENCES [dbo].[IPCASE] ([IPCASEID]),
    CONSTRAINT [FK_IPCASEPARCEL_PARCEL] FOREIGN KEY ([PARCELID]) REFERENCES [dbo].[PARCEL] ([PARCELID])
);

