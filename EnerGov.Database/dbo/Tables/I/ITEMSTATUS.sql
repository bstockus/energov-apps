CREATE TABLE [dbo].[ITEMSTATUS] (
    [ITEMSTATUSID] CHAR (36)     NOT NULL,
    [NAME]         VARCHAR (50)  NOT NULL,
    [DESCRIPTION]  VARCHAR (MAX) NULL,
    CONSTRAINT [PK_ItemStatus] PRIMARY KEY CLUSTERED ([ITEMSTATUSID] ASC) WITH (FILLFACTOR = 90)
);

