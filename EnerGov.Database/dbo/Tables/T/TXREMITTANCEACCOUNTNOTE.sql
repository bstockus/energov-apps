CREATE TABLE [dbo].[TXREMITTANCEACCOUNTNOTE] (
    [TXREMITTANCEACCOUNTNOTEID] CHAR (36)     NOT NULL,
    [TXREMITTANCEACCOUNTID]     CHAR (36)     NOT NULL,
    [TEXT]                      VARCHAR (MAX) NOT NULL,
    [CREATEDBY]                 CHAR (36)     NOT NULL,
    [CREATEDDATE]               DATETIME      NOT NULL,
    [TITLE]                     VARCHAR (50)  CONSTRAINT [DF_TXREMITTANCEACCOUNTNOTE_TITLE] DEFAULT ('') NULL,
    [PIN]                       BIT           CONSTRAINT [DF_TXREMITTANCEACCOUNTNOTE_PIN] DEFAULT ((0)) NOT NULL,
    PRIMARY KEY CLUSTERED ([TXREMITTANCEACCOUNTNOTEID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_TXACCOUNTNOTECREATEDBY] FOREIGN KEY ([CREATEDBY]) REFERENCES [dbo].[USERS] ([SUSERGUID]),
    CONSTRAINT [FK_TXACCOUNTNOTETXREMITTANCEACCOUNTID] FOREIGN KEY ([TXREMITTANCEACCOUNTID]) REFERENCES [dbo].[TXREMITTANCEACCOUNT] ([TXREMITTANCEACCOUNTID])
);


GO
CREATE NONCLUSTERED INDEX [IX_TXREMITTANCEACCOUNTNOTE_TXREMITTANCEACCOUNTID]
    ON [dbo].[TXREMITTANCEACCOUNTNOTE]([TXREMITTANCEACCOUNTID] ASC);

