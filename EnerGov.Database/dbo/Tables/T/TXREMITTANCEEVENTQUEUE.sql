CREATE TABLE [dbo].[TXREMITTANCEEVENTQUEUE] (
    [TXREMITTANCEEVENTQUEUEID]  BIGINT        IDENTITY (1, 1) NOT NULL,
    [TXREMITTANCEACCOUNTID]     CHAR (36)     NOT NULL,
    [TXREMITTANCEID]            CHAR (36)     NULL,
    [REMITTANCEACCOUNTNUMBER]   NVARCHAR (50) NULL,
    [TXREMITTANCEEVENTTYPEID]   INT           NOT NULL,
    [EVENTSTATUSID]             INT           NOT NULL,
    [CREATEDDATE]               DATETIME      NOT NULL,
    [PROCESSEDDATE]             DATETIME      NULL,
    [TXREMITTANCELASTCHANGEDBY] CHAR (36)     NULL,
    CONSTRAINT [PK_TXREMITTANCEEVENTQUEUE] PRIMARY KEY CLUSTERED ([TXREMITTANCEEVENTQUEUEID] ASC),
    CONSTRAINT [FK_TXREMITTANCEEVENTQUEUE_EventStatus] FOREIGN KEY ([EVENTSTATUSID]) REFERENCES [dbo].[EVENTSTATUS] ([EVENTSTATUSID]),
    CONSTRAINT [FK_TXREMITTANCEEVENTQUEUE_TXREMITTANCEEVENTTYPE] FOREIGN KEY ([TXREMITTANCEEVENTTYPEID]) REFERENCES [dbo].[TXREMITTANCEEVENTTYPE] ([TXREMITTANCEEVENTTYPEID]),
    CONSTRAINT [FK_TXREMITTANCEEVENTQUEUE_Users] FOREIGN KEY ([TXREMITTANCELASTCHANGEDBY]) REFERENCES [dbo].[USERS] ([SUSERGUID])
);


GO
CREATE NONCLUSTERED INDEX [IX_TXREMITTANCEEVENTQUEUE_CREATEDDATE]
    ON [dbo].[TXREMITTANCEEVENTQUEUE]([CREATEDDATE] ASC);


GO
CREATE NONCLUSTERED INDEX [TXREMITTANCEEVENTQUEUE_PROCESSEDDATE]
    ON [dbo].[TXREMITTANCEEVENTQUEUE]([PROCESSEDDATE] ASC);

