CREATE TABLE [dbo].[TXREMITTANCEFEE] (
    [TXREMITTANCEFEEID] CHAR (36) NOT NULL,
    [TXREMITTANCEID]    CHAR (36) NOT NULL,
    [CACOMPUTEDFEEID]   CHAR (36) NOT NULL,
    [CREATEDON]         DATETIME  NOT NULL,
    PRIMARY KEY CLUSTERED ([TXREMITTANCEFEEID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_FEE_ACCOUNTID] FOREIGN KEY ([TXREMITTANCEID]) REFERENCES [dbo].[TXREMITTANCE] ([TXREMITTANCEID]),
    CONSTRAINT [FK_FEE_COMPUTEDFEEID] FOREIGN KEY ([CACOMPUTEDFEEID]) REFERENCES [dbo].[CACOMPUTEDFEE] ([CACOMPUTEDFEEID])
);

