CREATE TABLE [dbo].[TXREMACCACTIVITY] (
    [TXREMACCACTIVITYID]         CHAR (36)     NOT NULL,
    [TXREMITTANCEACCOUNTID]      CHAR (36)     NOT NULL,
    [TXREMITTANCEACTIVITYTYPEID] CHAR (36)     NOT NULL,
    [REMACTIVITYNAME]            VARCHAR (250) NOT NULL,
    [REMACTIVITYNUMBER]          VARCHAR (250) NOT NULL,
    [REMACTIVITYCOMMENTS]        VARCHAR (MAX) NULL,
    [SUSERGUID]                  CHAR (36)     NOT NULL,
    [CREATEDON]                  DATETIME      NOT NULL,
    PRIMARY KEY CLUSTERED ([TXREMACCACTIVITYID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_TXACCCACTIVITYSUSERGUID] FOREIGN KEY ([SUSERGUID]) REFERENCES [dbo].[USERS] ([SUSERGUID]),
    CONSTRAINT [FK_TXACCCACTIVITYTXREMITTANCEACCOUNTID] FOREIGN KEY ([TXREMITTANCEACCOUNTID]) REFERENCES [dbo].[TXREMITTANCEACCOUNT] ([TXREMITTANCEACCOUNTID]),
    CONSTRAINT [FK_TXACCCACTIVITYTXREMITTANCEACTIVITYTYPEID] FOREIGN KEY ([TXREMITTANCEACTIVITYTYPEID]) REFERENCES [dbo].[BLLICENSEACTIVITYTYPE] ([BLLICENSEACTIVITYTYPEID])
);

