CREATE TABLE [dbo].[USER_HOLIDAY] (
    [GID]         UNIQUEIDENTIFIER CONSTRAINT [DF_User_Holiday_gID] DEFAULT (newid()) NOT NULL,
    [FKUSERID]    CHAR (36)        NOT NULL,
    [SSTART_DATE] DATETIME         NOT NULL,
    [SEND_DATE]   DATETIME         NOT NULL,
    CONSTRAINT [PK_User_Holiday] PRIMARY KEY NONCLUSTERED ([GID] ASC) WITH (FILLFACTOR = 90),
    CONSTRAINT [FK_User_Holiday_Users] FOREIGN KEY ([FKUSERID]) REFERENCES [dbo].[USERS] ([SUSERGUID])
);

