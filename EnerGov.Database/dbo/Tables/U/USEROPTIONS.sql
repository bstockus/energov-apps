CREATE TABLE [dbo].[USEROPTIONS] (
    [GOPTION]       CHAR (36)     CONSTRAINT [DF_UserOptions_gOption] DEFAULT (newid()) NOT NULL,
    [SNAME]         NVARCHAR (25) NOT NULL,
    [SDISPLAYVALUE] NVARCHAR (25) NOT NULL,
    CONSTRAINT [PK_UserOptions] PRIMARY KEY CLUSTERED ([GOPTION] ASC) WITH (FILLFACTOR = 90)
);

