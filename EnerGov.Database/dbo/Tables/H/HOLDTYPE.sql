CREATE TABLE [dbo].[HOLDTYPE] (
    [HOLDTYPEID]   INT          NOT NULL,
    [HOLDTYPENAME] VARCHAR (50) NULL,
    CONSTRAINT [PK_HoldType] PRIMARY KEY CLUSTERED ([HOLDTYPEID] ASC) WITH (FILLFACTOR = 90)
);

