CREATE TABLE [dbo].[HOLDMODULELIST] (
    [MODULEID]   INT            NOT NULL,
    [MODULENAME] NVARCHAR (100) NULL,
    CONSTRAINT [PK_HoldModuleList] PRIMARY KEY CLUSTERED ([MODULEID] ASC) WITH (FILLFACTOR = 90)
);

