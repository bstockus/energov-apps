CREATE PROCEDURE [maps].[USP_PARCEL_GETBY_PARCELNUMBER]
(
	@PARCELNUMBER NVARCHAR(100)
)
AS
BEGIN	
	SET NOCOUNT ON;
	 SELECT [PARCEL].[PARCELID],
			[PARCEL].[PARCELNUMBER],
			[PARCEL].[NAME1],
			[PARCEL].[NAME2]			
		FROM 
			[dbo].[PARCEL]
		WHERE [PARCEL].[PARCELNUMBER] = @PARCELNUMBER
END