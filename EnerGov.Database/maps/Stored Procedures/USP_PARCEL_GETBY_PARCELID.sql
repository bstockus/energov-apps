CREATE PROCEDURE [maps].[USP_PARCEL_GETBY_PARCELID]
(
	@PARCELID CHAR(36)
)
AS
BEGIN	
	SET NOCOUNT ON;
	 SELECT [PARCEL].[PARCELID],
			[PARCEL].[PARCELNUMBER],
			[PARCEL].[NAME1],
			[PARCEL].[NAME2]			
		FROM 
			[dbo].[PARCEL]
		WHERE [PARCEL].PARCELID = @PARCELID
END