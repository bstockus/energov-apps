CREATE PROCEDURE [gissetupapp].[USP_GEOQUERYUNITTYPE_SELECT_LOOKUP]
AS
BEGIN
SET NOCOUNT ON;
SELECT 
	[dbo].[GEOQUERYUNITTYPE].[GEOQUERYUNITTYPEID],
	[dbo].[GEOQUERYUNITTYPE].[QUERYUNITTYPENAME]
FROM 
	[dbo].[GEOQUERYUNITTYPE]
ORDER BY 
	[dbo].[GEOQUERYUNITTYPE].[QUERYUNITTYPENAME]

END